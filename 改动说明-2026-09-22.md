# 改动说明 · 2026-09-22

采纳走查提出的三条建议。全部改动**不涉及数据结构**，不需要重新导出备份，
原有阳光 / 水滴 / 任务 / 进度全部保留。

回归结果：`_smoke.js` 310 通过 / 0 失败，孩子端 0 报错，妈妈端 0 报错，
死按钮 0 个，幽灵动作 0 个，670 个页面场景渲染正常。

---

## 建议 1 · 删除操作加二次确认

**改的文件：`js/parent.js`**（妈妈端）

| 位置 | 原来 | 现在 |
|---|---|---|
| `act` 分支 `delTask` | 点一下立刻删 | 先弹确认框，点「删掉」才真删 |
| `act` 分支 `delShop` | 点一下立刻删 | 先弹确认框，点「删掉」才真删 |
| `act` 分支 `mcClear` | 点一下立刻清零 | 先弹确认框，点「清零」才执行 |

- 确认框里会带上**要删的那个东西的名字**，避免删错行
- 删奖励的提示里多一句：「如果只是暂时不想给，可以先把价格调高」
- 三个都从 `return true` 改成 `return false`（真正动作挪进确认回调里）
- 回调里补了 `global.App.render()` —— 确认框是异步的，不走原来的重绘分支

没改的地方：`import`（粘贴文字恢复）本来就有弹窗，必须手动粘一大段文字再点
「导入并覆盖」，误触概率极低，保持原样。

验证：`node _tools/conftest.js` —— 模拟「点一下但不点确定」，确认数据没动；
再点确定，才真的删除。三处均通过。

---

## 建议 2 · 孩子端按钮统一到 52px

**改的文件：`js/kid.js`（25 处）、`js/math.js`（1 处）**

把所有 `min-height:44px` / `48px` / `50px` 改成 `52px`，共 26 处。
涉及：各内页的「← 返回」「换一课」「回关卡」、预习的「给我一条线索」、
背诵的「重来一次」「想不起来看一下」、自加任务的一句话小按钮等。

**妈妈端 `js/parent.js` 没动** —— 是大人用的，44/48 更紧凑，抬高了反而占地方。

改完后孩子端只剩 52（普通）/ 56（主按钮）/ 96 和 130（大卡片），没有低于 52 的了。

---

## 建议 3 · 删掉没人用的旧分支

**改的文件：`js/kid.js`**

删掉了 `act` 里的 `pvUnit` 分支。选课实际走的是 `pvPick`（目录里点哪一课就是哪一课），
`pvUnit` 从来没有任何按钮会触发它，属于历史遗留。

删掉后，走查脚本报的「幽灵动作」从 2 个降到 **0 个**。

---

## 怎么验证改动没影响老功能

```bash
export PATH="/usr/bin:/bin:/c/Windows/System32:/c/WINDOWS"
cd "D:/Micky工作台/pvz-study"
N="C:/Users/ThinkPad/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
"$N" _smoke.js                # 310 通过 / 0 失败
"$N" _childwalk.js            # 孩子端 3711 次操作
"$N" _parentwalk.js           # 妈妈端 26 个操作 + 完整闭环
"$N" _tools/actaudit.js       # 死按钮 0
"$N" _tools/conftest.js       # 删除确认真的拦得住
"$N" _warmtest.js            # 五个暖心小功能（52 项）
```
